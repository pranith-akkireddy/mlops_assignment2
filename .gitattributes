# Data, models, and artifacts are versioned with DVC (see dvc.yaml), not Git LFS.
# Declaring LFS filters on the same paths would conflict with DVC's own
# pointer files, so only line-ending normalisation is configured here.
* text=auto eol=lf
*.png binary
*.jpg binary
*.pkl binary
*.npz binary
